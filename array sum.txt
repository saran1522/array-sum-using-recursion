// ************sum of array********
int sumarray(int arr[], int n)
{
  int sum=arr[n];
   if(n==0)
    return arr[0];
  return sum+sumarray(arr, n-1);
}
int main()
{
  int n;
  cin>>n;
  int arr[n];
  for(int i=0; i<n; i++)
  cin>>arr[i];
  int sum=sumarray(arr,--n);
  cout<<sum;
}